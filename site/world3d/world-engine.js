import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { CharacterActor } from './character-actor.js?v=163';
import { NavigationGrid } from './navigation-grid.mjs';
import { NPCManager } from './npc-manager.js?v=163';
import { Combat3DController } from './combat-controller.js?v=163';
import { AbilityEffects } from './ability-effects.js?v=141';
import { Party3DManager } from './party-manager.js?v=163';
import { Chronicle3DAdapter } from './chronicle-adapter.js?v=151';
import { WorldPolish } from './world-polish.js';
import { CinematicDirector } from './cinematic-director.js?v=145';
import { CityAtmosphere } from './city-atmosphere.mjs?v=162';
import { CameraObstruction } from './camera-obstruction.mjs?v=159';
import { WorldPerformanceManager } from './world-performance.mjs?v=144';
import { environmentAssetLoadStats } from './environment-asset-loader.js?v=144';
import { CAMERA_KEY_DIRECTIONS, cameraPanStep, clampCameraPan } from './camera-input.mjs?v=166';

export class WorldEngine extends EventTarget {
  constructor({canvas,overlay,zoneFactory,character}) {
    super(); this.canvas=canvas; this.overlay=overlay; this.zoneFactory=zoneFactory; this.characterConfig=character;
    this.clock=new THREE.Clock(); this.running=false; this.raycaster=new THREE.Raycaster(); this.pointer=new THREE.Vector2();this.cameraTargetBefore=new THREE.Vector3();this.cameraFollowDelta=new THREE.Vector3();
    this.cameraKeys=new Set();this.cameraPanOffset=new THREE.Vector3();this.cameraForward=new THREE.Vector3();this.cameraRight=new THREE.Vector3();
    this.interactionObjects=[]; this.pendingInteraction=null;
  }

  async initialize() {
    this.renderer=new THREE.WebGLRenderer({canvas:this.canvas,antialias:true,powerPreference:'high-performance'});
    this.renderer.setPixelRatio(Math.min(devicePixelRatio,1.5)); this.renderer.shadowMap.enabled=true; this.renderer.shadowMap.type=THREE.PCFSoftShadowMap;
    this.renderer.toneMapping=THREE.ACESFilmicToneMapping;this.renderer.toneMappingExposure=1.12;this.renderer.outputColorSpace=THREE.SRGBColorSpace;
    this.camera=new THREE.PerspectiveCamera(44,1,.1,180);this.camera.position.set(7.2,7.2,9.2);
    this.controls=new OrbitControls(this.camera,this.canvas);this.controls.enableDamping=true;this.controls.enablePan=false;this.controls.minDistance=5.5;this.controls.maxDistance=18;this.controls.minPolarAngle=.42;this.controls.maxPolarAngle=1.12;
    this.scene=new THREE.Scene();this.scene.background=new THREE.Color(0x10201d);this.scene.fog=new THREE.FogExp2(0x10201d,.025);
    this.hemisphereLight=new THREE.HemisphereLight(0xc4d4bd,0x30281f,2.35);this.scene.add(this.hemisphereLight);
    this.sunLight=new THREE.DirectionalLight(0xffefd2,2.8);this.sunLight.position.set(-9,18,10);this.sunLight.castShadow=true;this.sunLight.shadow.mapSize.set(2048,2048);this.sunLight.shadow.camera.left=-24;this.sunLight.shadow.camera.right=24;this.sunLight.shadow.camera.top=24;this.sunLight.shadow.camera.bottom=-24;this.sunLight.shadow.bias=-.0003;this.scene.add(this.sunLight);
    this.zone=this.zoneFactory();if(this.zone.scene){this.scene.background=new THREE.Color(this.zone.scene.background);this.scene.fog=new THREE.FogExp2(this.zone.scene.fog,this.zone.scene.fogDensity??.025);this.renderer.toneMappingExposure=this.zone.scene.exposure??1.12;this.hemisphereLight.intensity=this.zone.scene.ambientIntensity??2.35;this.sunLight.intensity=this.zone.scene.sunIntensity??2.8;}this.scene.add(this.zone.root);this.worldLabels=[];this.worldLabelPosition=new THREE.Vector3();this.zone.root.traverse(object=>{if(Array.isArray(object.userData?.worldLabelBase))this.worldLabels.push(object);});
    this.navigation=new NavigationGrid({...this.zone.bounds,cellSize:.65,padding:.62,obstacles:this.zone.obstacles});for(const record of this.zone.interactables)for(const action of record.actions||[])window.registerAuthoredEnvironmentAction?.(this.zone.id,record.id,action);
    this.interactionObjects=this.zone.interactables.map(i=>i.object);
    this.actor=new CharacterActor(this.characterConfig);this.actor.position.copy(this.restoredPosition());this.actor.rotation.y=this.restoredRotation();this.scene.add(this.actor);this.controls.target.copy(this.actor.position).add(new THREE.Vector3(0,1.25,0));this.camera.position.copy(this.actor.position).add(this.zone.cameraOffset||new THREE.Vector3(0,7.5,8));this.controls.update();
    this.actor.addEventListener('statechange',e=>this.setStateLabel(e.state));
    this.buildOverlay();this.bindInput();this.resize();if(this.zone.ready){const detail=this.loadingElement?.querySelector('span');if(detail)detail.textContent='Loading authored buildings and forest…';this.loadingProgressTimer=setInterval(()=>{if(!detail)return;const stats=environmentAssetLoadStats();detail.textContent=stats.queued||stats.active?`Loading world assets · ${stats.active} active · ${stats.queued} queued`:'Finalizing navigation and lighting…';},180);try{await this.zone.ready;}finally{clearInterval(this.loadingProgressTimer);this.loadingProgressTimer=null;}}await this.actor.load();if(this.zone.npcs?.length){this.npcManager=new NPCManager(this,this.zone.npcs);await this.npcManager.initialize();}this.abilityEffects=new AbilityEffects(this);this.combatController=new Combat3DController(this);this.partyManager=new Party3DManager(this);await this.partyManager.initialize();this.cinematicDirector=new CinematicDirector(this);this.chronicleAdapter=new Chronicle3DAdapter(this);this.chronicleAdapter.initialize();this.worldPolish=new WorldPolish(this);this.worldPolish.initialize();this.cityAtmosphere=new CityAtmosphere(this).initialize();this.cameraObstruction=new CameraObstruction(this);this.performanceManager=new WorldPerformanceManager(this);window.AudioEngine?.startCityAmbience?.({zoneId:this.zone.id});this.loadingElement?.remove();this.setStateLabel('idle');this.updateVitals(true);const hpPercent=Math.round((Number(window.gameState?.character?.hp)||0)/Math.max(1,Number(window.gameState?.character?.maxHp)||1)*100);this.toast(hpPercent<=50?`You are wounded (${hpPercent}% HP). Rest at camp before a dangerous encounter.`:'Click the ground to move. Shift-click to run.',hpPercent<=50?5200:3200);
    this.dispatchEvent(new Event('ready')); return this;
  }

  buildOverlay(){
    this.overlay.replaceChildren();
    const hud=document.createElement('div');hud.className='world3d-hud';hud.innerHTML=`<div class="world3d-loading"><i></i><strong>ENTERING ${this.zone.name.toUpperCase()}</strong><span>Preparing the world…</span></div><div class="world3d-title"><span>EXPLORATION · ${this.actor.profile.name.toUpperCase()} ${this.actor.classProfile.name.toUpperCase()}</span><strong>${this.zone.name}</strong></div><button class="world3d-class-action" type="button"><kbd>1</kbd><span>${this.actor.classProfile.action}</span></button><nav class="world3d-utility-bar" aria-label="Game menus"><button type="button" data-world-action="character" title="Character (C)" aria-label="Character"><span aria-hidden="true">♙</span><small>Character</small></button><button type="button" data-world-action="inventory" title="Inventory (I)" aria-label="Inventory"><span aria-hidden="true">▣</span><small>Inventory</small></button><button type="button" data-world-action="journal" title="Journal (J)" aria-label="Journal"><span aria-hidden="true">☷</span><small>Journal</small></button><button type="button" data-world-action="map" title="Map (M)" aria-label="Map"><span aria-hidden="true">◇</span><small>Map</small></button><button type="button" data-world-action="camp" title="Camp" aria-label="Camp"><span aria-hidden="true">♨</span><small>Camp</small></button><button type="button" data-world-action="settings" title="Settings" aria-label="Settings"><span aria-hidden="true">⚙</span><small>Settings</small></button></nav><div class="world3d-state"><i></i><span>IDLE</span></div><div class="world3d-help">W/S camera forward/back <b>·</b> A/D camera right/left <b>·</b> Click to move <b>·</b> Drag to orbit</div><div class="world3d-prompt" hidden><button type="button"><kbd>E</kbd><span></span></button></div><section class="world3d-interaction-menu" role="dialog" aria-modal="false" aria-label="Environmental actions" hidden><header><span>INTERACT</span><strong></strong><button type="button" data-env-close aria-label="Close environmental actions">×</button></header><div class="world3d-interaction-choices"></div><form class="world3d-custom-action"><label for="world3d-custom-action-input">Try something else…</label><div><input id="world3d-custom-action-input" maxlength="240" autocomplete="off" placeholder="Describe what you do with this object"><button type="submit">Try</button></div></form></section><div class="world3d-toast" role="status" aria-live="polite"></div>`;
    const vitals=document.createElement('section');vitals.className='world3d-vitals';vitals.setAttribute('aria-label','Character vitality');vitals.innerHTML=`<header><strong>${this.actor.profile.name}</strong><span class="w3v-condition">READY</span></header><div class="w3v-row hp"><span>HP</span><div><i></i></div><b>0 / 0</b></div><div class="w3v-row mp"><span>MP</span><div><i></i></div><b>0 / 0</b></div><button type="button" aria-label="Rest at Camp">♨ REST AT CAMP</button>`;hud.querySelector('.world3d-class-action').after(vitals);
    this.overlay.appendChild(hud);this.loadingElement=hud.querySelector('.world3d-loading');this.stateLabel=hud.querySelector('.world3d-state span');this.prompt=hud.querySelector('.world3d-prompt');this.promptText=this.prompt.querySelector('span');this.toastElement=hud.querySelector('.world3d-toast');this.interactionMenu=hud.querySelector('.world3d-interaction-menu');this.interactionTitle=this.interactionMenu.querySelector('header strong');this.interactionChoices=this.interactionMenu.querySelector('.world3d-interaction-choices');this.customActionForm=this.interactionMenu.querySelector('form');this.customActionInput=this.customActionForm.querySelector('input');
    this.classActionButton=hud.querySelector('.world3d-class-action');this.classActionText=this.classActionButton.querySelector('span');this.vitalsElement=vitals;this.vitalsCondition=vitals.querySelector('.w3v-condition');this.vitalsHpFill=vitals.querySelector('.w3v-row.hp i');this.vitalsHpText=vitals.querySelector('.w3v-row.hp b');this.vitalsMpFill=vitals.querySelector('.w3v-row.mp i');this.vitalsMpText=vitals.querySelector('.w3v-row.mp b');this.vitalsCampButton=vitals.querySelector('button');this.utilityBar=hud.querySelector('.world3d-utility-bar');this.prompt.querySelector('button').addEventListener('click',()=>this.confirmInteraction());this.interactionMenu.querySelector('[data-env-close]').addEventListener('click',()=>this.closeInteractionMenu());this.interactionChoices.addEventListener('click',event=>{const button=event.target.closest('[data-env-action]');if(button)this.selectInteractionAction(Number(button.dataset.envAction));});this.customActionForm.addEventListener('submit',event=>{event.preventDefault();this.submitCustomEnvironmentAction();});this.classActionButton.addEventListener('click',()=>this.playClassAction());this.vitalsCampButton.addEventListener('click',()=>this.openUtility('camp'));this.utilityBar.addEventListener('click',event=>{const button=event.target.closest('[data-world-action]');if(button)this.openUtility(button.dataset.worldAction);});
  }

  openUtility(action){
    if(this.combatController?.active)return;
    const actions={
      character:()=>window.openCharSheet?.(),
      inventory:()=>window.openCharSheet?.('inventory'),
      journal:()=>window.showQuestLog?.(),
      map:()=>window.openWorldMap?.(),
      camp:()=>window.dispatchEvent(new CustomEvent('sanctum:open-camp')),
      settings:()=>window.SanctumUI?.openAccessibilityPanel?.()
    };
    const handler=actions[action];if(handler)handler();
  }

  setStateLabel(state){if(this.stateLabel)this.stateLabel.textContent=String(state).toUpperCase();}
  updateVitals(force=false){
    const character=window.gameState?.character||{},combatant=this.combatController?.active?window.combatState?.combatants?.player:null,source=combatant||character;
    const hp=Math.max(0,Number(source.hp)||0),maxHp=Math.max(1,Number(source.maxHp)||Number(character.maxHp)||1),mp=Math.max(0,Number(source.mp)||0),maxMp=Math.max(0,Number(source.maxMp)||Number(character.maxMp)||0),inCombat=!!this.combatController?.active;
    const hpPercent=Math.round(hp/maxHp*100),mpPercent=maxMp?Math.round(mp/maxMp*100):0,condition=hpPercent<=25?'CRITICAL':hpPercent<=50?'WOUNDED':'READY',signature=[hp,maxHp,mp,maxMp,condition,inCombat].join(':');if(!force&&signature===this.vitalsSignature)return;this.vitalsSignature=signature;
    this.vitalsElement?.classList.toggle('wounded',hpPercent<=50);this.vitalsElement?.classList.toggle('critical',hpPercent<=25);if(this.vitalsCondition)this.vitalsCondition.textContent=condition;if(this.vitalsHpFill)this.vitalsHpFill.style.width=`${Math.max(0,Math.min(100,hpPercent))}%`;if(this.vitalsHpText)this.vitalsHpText.textContent=`${hp} / ${maxHp}`;if(this.vitalsMpFill)this.vitalsMpFill.style.width=`${Math.max(0,Math.min(100,mpPercent))}%`;if(this.vitalsMpText)this.vitalsMpText.textContent=maxMp?`${mp} / ${maxMp}`:'—';if(this.vitalsCampButton){this.vitalsCampButton.hidden=hpPercent>50||inCombat;this.vitalsCampButton.disabled=inCombat;}this.vitalsElement?.setAttribute('aria-label',`Character vitality: ${hp} of ${maxHp} HP${maxMp?`, ${mp} of ${maxMp} MP`:''}. ${condition}.`);
  }
  restoredPosition(){if(new URLSearchParams(location.search).has('fresh3d'))return this.zone.spawn;const saved=window.gameState?.world3dPositions?.[this.zone.id];if(!saved||!Number.isFinite(saved.x)||!Number.isFinite(saved.z))return this.zone.spawn;const point={x:THREE.MathUtils.clamp(saved.x,this.zone.bounds.minX,this.zone.bounds.maxX),z:THREE.MathUtils.clamp(saved.z,this.zone.bounds.minZ,this.zone.bounds.maxZ)};const cell=this.navigation.nearestOpen(this.navigation.cellAt(point)),open=this.navigation.pointAt(cell);return new THREE.Vector3(open.x,0,open.z);}
  restoredRotation(){const rotation=window.gameState?.world3dPositions?.[this.zone.id]?.rotation;return Number.isFinite(rotation)?rotation:0;}
  persistPosition(){if(!this.actor||!window.gameState)return;window.gameState.world3dPositions=window.gameState.world3dPositions||{};window.gameState.world3dPositions[this.zone.id]={x:Number(this.actor.position.x.toFixed(3)),z:Number(this.actor.position.z.toFixed(3)),rotation:Number(this.actor.rotation.y.toFixed(3))};}
  toast(message,duration=3200){if(!this.toastElement)return;this.toastElement.textContent=message;this.toastElement.classList.add('visible');clearTimeout(this.toastTimer);this.toastTimer=setTimeout(()=>this.toastElement?.classList.remove('visible'),duration);}
  bindInput(){
    let down=null;this.onPointerDown=e=>{down={x:e.clientX,y:e.clientY};};
    this.onPointerUp=e=>{if(!down||Math.hypot(e.clientX-down.x,e.clientY-down.y)>7){down=null;return;}down=null;this.handleTap(e);};
    this.onKey=e=>{if(!this.running)return;const editing=/INPUT|TEXTAREA|SELECT/.test(document.activeElement?.tagName||'');if(e.code==='Escape')this.cameraKeys.clear();if(this.cameraInputAllowed(editing)&&CAMERA_KEY_DIRECTIONS[e.code]){e.preventDefault();this.cameraKeys.add(e.code);}if(e.code==='Escape'&&!this.interactionMenu?.hidden){e.preventDefault();this.closeInteractionMenu();return;}if(e.code==='Escape'&&window.npcConvState?.active){e.preventDefault();window.closeConvPanel?.();return;}if(!editing&&this.combatController?.active&&this.combatController.handleKey(e.code)){e.preventDefault();return;}if((e.code==='KeyE'||e.code==='Enter')&&this.pendingInteraction&&!editing){e.preventDefault();this.confirmInteraction();}if(e.code==='Digit1'&&!editing&&!window.npcConvState?.active){e.preventDefault();this.playClassAction();}if(!editing&&!this.combatController?.active&&!window.npcConvState?.active){const shortcuts={KeyC:'character',KeyI:'inventory',KeyJ:'journal',KeyM:'map'};if(shortcuts[e.code]){e.preventDefault();this.openUtility(shortcuts[e.code]);}}};
    this.onKeyUp=e=>{if(CAMERA_KEY_DIRECTIONS[e.code])this.cameraKeys.delete(e.code);};this.onWindowBlur=()=>this.cameraKeys.clear();
    this.onResize=()=>this.resize();this.canvas.addEventListener('pointerdown',this.onPointerDown);this.canvas.addEventListener('pointerup',this.onPointerUp);addEventListener('keydown',this.onKey);addEventListener('keyup',this.onKeyUp);addEventListener('blur',this.onWindowBlur);addEventListener('resize',this.onResize);
  }

  cameraInputAllowed(editing=false){return this.running&&!editing&&!this.combatController?.active&&!window.npcConvState?.active&&!document.getElementById('scene-panel')&&!document.getElementById('esc-menu')&&this.interactionMenu?.hidden!==false&&!this.cinematicDirector?.active;}
  updateCameraPan(dt){
    if(!this.cameraInputAllowed())this.cameraKeys.clear();
    if(!this.cameraKeys.size)return;
    const step=cameraPanStep(this.cameraKeys,dt,8.5);if(!step.forward&&!step.right)return;
    this.camera.getWorldDirection(this.cameraForward);this.cameraForward.y=0;if(this.cameraForward.lengthSq()<.001)this.cameraForward.set(0,0,-1);else this.cameraForward.normalize();
    this.cameraRight.crossVectors(this.cameraForward,this.camera.up).normalize();
    this.cameraPanOffset.addScaledVector(this.cameraForward,step.forward).addScaledVector(this.cameraRight,step.right);
    const clamped=clampCameraPan(this.cameraPanOffset.x,this.cameraPanOffset.z,14);this.cameraPanOffset.set(clamped.x,0,clamped.z);
  }

  ndc(e){const r=this.canvas.getBoundingClientRect();return new THREE.Vector2(((e.clientX-r.left)/r.width)*2-1,-((e.clientY-r.top)/r.height)*2+1);}
  handleTap(e){
    if(window.npcConvState?.active||document.getElementById('scene-panel'))return;
    if(this.combatController?.handleTap(e,this.raycaster))return;
    this.raycaster.setFromCamera(this.ndc(e),this.camera);
    const interactionHit=this.raycaster.intersectObjects(this.interactionObjects,true)[0];
    if(interactionHit){const record=this.zone.interactables.find(i=>i.object===interactionHit.object||i.object===interactionHit.object.parent);if(record){this.goToInteraction(record,e.shiftKey);return;}}
    const groundHit=this.raycaster.intersectObject(this.zone.ground,true)[0];if(!groundHit)return;
    const target=groundHit.point;target.x=THREE.MathUtils.clamp(target.x,this.zone.bounds.minX,this.zone.bounds.maxX);target.z=THREE.MathUtils.clamp(target.z,this.zone.bounds.minZ,this.zone.bounds.maxZ);
    this.pendingInteraction=null;this.hidePrompt();this.closeInteractionMenu();this.moveActor(target,e.shiftKey);
  }
  moveActor(target,run=false,onArrive=null){const path=this.navigation.findPath(this.actor.position,target);if(!path.length){this.toast('That route is blocked.');return false;}const destination=path.at(-1);this.marker.position.set(destination.x,destination.y||0,destination.z);this.marker.visible=true;this.actor.moveAlong(path,{run,onArrive:()=>{this.marker.visible=false;this.persistPosition();if(onArrive)onArrive();}});return true;}
  interactionApproach(record){const radius=Math.max(.85,(record.range||1.8)*.72),origin=record.position,bearing=Math.atan2(this.actor.position.x-origin.x,this.actor.position.z-origin.z);let best=null,bestCost=Infinity;for(let index=0;index<16;index++){const angle=bearing+index*Math.PI/8,candidate={x:origin.x+Math.sin(angle)*radius,y:0,z:origin.z+Math.cos(angle)*radius};if(this.navigation.isPointBlocked(candidate))continue;const path=this.navigation.findPath(this.actor.position,candidate);if(!path.length)continue;let cost=0;for(let step=1;step<path.length;step++)cost+=Math.hypot(path[step].x-path[step-1].x,path[step].z-path[step-1].z);cost+=index*.015;if(cost<bestCost){bestCost=cost;best=candidate;}}return best;}
  goToInteraction(record,run=false){this.closeInteractionMenu();this.pendingInteraction=null;this.hidePrompt();const stop=this.interactionApproach(record);if(!stop){this.toast('There is no clear way to reach that interaction.');return;}this.moveActor(stop,run,()=>{this.actor.turnToward(record.position);this.actor.playOneShot('interact');this.pendingInteraction=record;this.showPrompt(record);});}
  showPrompt(record){this.promptText.textContent=record.label;this.prompt.querySelector('button').setAttribute('aria-label',`Interact: ${record.label}`);this.prompt.hidden=false;}
  hidePrompt(){this.prompt.hidden=true;}
  presentInteraction(record){if(!record||String(record.id).startsWith('npc:'))return;this.cinematicDirector?.playMoment('environment',this.actor,record.position,{duration:.82,caption:record.label});}
  confirmInteraction(){const record=this.pendingInteraction;if(!record)return;this.pendingInteraction=null;this.hidePrompt();const action=record.actions?.[0];if(Array.isArray(record.actions)&&(record.actions.length>1||action&&!action.direct)){this.openInteractionMenu(record);return;}this.presentInteraction(record);if(action?.onSelect)action.onSelect(this,record);else record.onInteract?.(this,action);}
  actionAvailable(action){return !action?.requiresFlag||!!window.sceneState?.flags?.[action.requiresFlag];}
  openInteractionMenu(record){this.activeInteraction=record;this.interactionTitle.textContent=record.label;this.interactionChoices.replaceChildren();for(const [index,action] of record.actions.entries()){const available=this.actionAvailable(action),button=document.createElement('button');button.type='button';button.dataset.envAction=String(index);button.disabled=!available;const icon=document.createElement('span');icon.textContent=action.icon||'◆';const copy=document.createElement('span');const label=document.createElement('strong');label.textContent=action.label;copy.appendChild(label);if(action.check||!available){const detail=document.createElement('small');detail.textContent=available?`${String(action.check.skill||action.check.ability).replaceAll('_',' ').toUpperCase()} · DC ${action.check.dc}`:'REQUIRES EVIDENCE';copy.appendChild(detail);}button.append(icon,copy);this.interactionChoices.appendChild(button);}this.customActionInput.value='';this.interactionMenu.hidden=false;}
  closeInteractionMenu(){if(this.interactionMenu)this.interactionMenu.hidden=true;this.activeInteraction=null;}
  async selectInteractionAction(index){const record=this.activeInteraction,action=record?.actions?.[index];if(!record||!action)return;if(!this.actionAvailable(action)){this.toast('You have not found the evidence needed for that approach.');return;}this.closeInteractionMenu();this.presentInteraction(record);if(action.direct){if(action.onSelect)action.onSelect(this,record);else record.onInteract?.(this,action);return;}const result=await window.resolveEnvironmentalAction?.({zoneId:this.zone.id,targetId:record.id,targetLabel:record.label,action});action.onResolved?.(result,this,record);if(result?.pending)this.toast('Waiting for the Session Master to resolve that action.');else if(result?.message)this.toast(result.message,4800);}

  transitionToWorldLocation(id,label='the next area'){
    if(this.transitioning)return false;this.transitioning=true;this.actor?.stop();this.closeInteractionMenu();this.hidePrompt();
    const veil=document.createElement('div');veil.className='world3d-transition';veil.innerHTML=`<span>ENTERING</span><strong>${String(label).toUpperCase()}</strong>`;this.overlay.appendChild(veil);requestAnimationFrame(()=>veil.classList.add('visible'));
    window.setTimeout(()=>{if(!window.travelToWorldLocation?.(id)){veil.remove();this.transitioning=false;this.toast('The way forward is unavailable.');}},520);return true;
  }
  async submitCustomEnvironmentAction(){const record=this.activeInteraction,text=this.customActionInput.value.trim();if(!record||!text)return;this.customActionInput.disabled=true;this.presentInteraction(record);try{const result=await window.resolveEnvironmentalAction?.({zoneId:this.zone.id,targetId:record.id,targetLabel:record.label,text});if(result?.pending){this.closeInteractionMenu();this.toast('Waiting for the Session Master to resolve that action.');return;}this.customActionInput.value='';if(result?.message)this.toast(result.message,4800);}catch(error){console.error('Environmental action failed:',error);this.toast('That action could not be resolved. Please try again.');}finally{this.customActionInput.disabled=false;}}
  playClassAction(){if(this.combatController?.active){this.combatController.attack();return;}if(this.actor.playPrimaryAction())this.toast(this.actor.classProfile.action,900);}
  registerInteraction(record){this.zone.interactables.push(record);this.interactionObjects.push(record.object);}

  makeMarker(){const group=new THREE.Group();const ring=new THREE.Mesh(new THREE.RingGeometry(.38,.5,32),new THREE.MeshBasicMaterial({color:0xe2c779,transparent:true,opacity:.9,side:THREE.DoubleSide,depthWrite:false}));ring.rotation.x=-Math.PI/2;ring.position.y=.04;group.add(ring);group.visible=false;this.scene.add(group);this.marker=group;this.markerRing=ring;}
  updateWorldLabels(){for(const label of this.worldLabels||[]){label.getWorldPosition(this.worldLabelPosition);const distance=this.camera.position.distanceTo(this.worldLabelPosition),base=label.userData.worldLabelBase;label.visible=distance>2.5;const factor=THREE.MathUtils.clamp(distance/18,.16,1);label.scale.set(base[0]*factor,base[1]*factor,base[2]);}}
  resize(){const w=this.canvas.clientWidth||innerWidth,h=this.canvas.clientHeight||innerHeight;this.renderer?.setSize(w,h,false);if(this.camera){this.camera.aspect=w/h;this.camera.updateProjectionMatrix();}}
  start(){if(this.running)return;this.running=true;if(!this.marker)this.makeMarker();this.clock.start();this.frame();}
  frame(){if(!this.running)return;const dt=Math.min(this.clock.getDelta(),.05),time=this.clock.elapsedTime;if(document.hidden){this.raf=requestAnimationFrame(()=>this.frame());return;}this.actor?.update(dt);if(!this.combatController?.active)this.npcManager?.update(dt,time);this.combatController?.update(dt);this.partyManager?.update(dt);this.chronicleAdapter?.update(dt);this.worldPolish?.update(dt);this.abilityEffects?.update(dt);this.cityAtmosphere?.update(dt,time);this.updateVitals();if(this.classActionText)this.classActionText.textContent=this.combatController?.active?'Attack Target':this.actor.classProfile.action;if(this.cinematicDirector?.active)this.cinematicDirector.update(dt);else{this.updateCameraPan(dt);if(this.actor){const focusActor=this.cinematicFocus||this.actor,focus=focusActor.position.clone().add(new THREE.Vector3(0,1.25,0)).add(this.cameraPanOffset);this.cameraTargetBefore.copy(this.controls.target);this.controls.target.lerp(focus,1-Math.exp(-dt*4.5));this.cameraFollowDelta.copy(this.controls.target).sub(this.cameraTargetBefore);this.camera.position.add(this.cameraFollowDelta);}this.controls.update();}this.cameraObstruction?.update(dt);this.performanceManager?.update(dt);if(this.marker?.visible)this.markerRing.rotation.z+=dt*1.8;this.zone?.update?.(time,dt);this.updateWorldLabels();this.renderer.render(this.scene,this.camera);this.raf=requestAnimationFrame(()=>this.frame());}
  stop(){this.running=false;cancelAnimationFrame(this.raf);}
  dispose(){this.persistPosition();this.stop();clearTimeout(this.toastTimer);clearInterval(this.loadingProgressTimer);this.cameraKeys.clear();this.canvas.removeEventListener('pointerdown',this.onPointerDown);this.canvas.removeEventListener('pointerup',this.onPointerUp);removeEventListener('keydown',this.onKey);removeEventListener('keyup',this.onKeyUp);removeEventListener('blur',this.onWindowBlur);removeEventListener('resize',this.onResize);this.cinematicDirector?.dispose();this.cameraObstruction?.dispose();this.performanceManager?.dispose();this.cityAtmosphere?.dispose();this.controls?.dispose();this.worldPolish?.dispose();this.chronicleAdapter?.dispose();this.combatController?.dispose();this.partyManager?.dispose();this.abilityEffects?.dispose();this.npcManager?.dispose();this.actor?.dispose();this.zone?.dispose?.();this.renderer?.dispose();this.overlay.replaceChildren();}
}
